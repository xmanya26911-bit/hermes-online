# Pinned to match hermes-agent v0.21.0 (backend/Dockerfile installs
# hermes-agent[web] from upstream git). DO NOT widen these independently:
# pip resolves one environment, so any mismatch with Hermes' exact pins
# (pyproject.toml) fails the Render build during dependency resolution.
fastapi==0.133.1
uvicorn[standard]==0.41.0
httpx==0.28.1
websockets==15.0.1
pyyaml==6.0.3
